<?php

// Enable Deploy Process
$enabled=false;

// Email Recipient
$to_email="<to@email.com>";

// Address Email will come from
$from_email="<from@email.com>";

// Site URL being deployed
$site_name="<your_site_name>";

// Whitelist Users?  (set valid_users if true)
$limit_users=false;

// Only deploy when there are changes to a specific branch(es)?
$limit_branch=false;

// do git pull??
$git_update=true;

//////////////////////////
// Optional
//////////////////////////

//$valid_users = array();
//$valid_branches = array();

?>
